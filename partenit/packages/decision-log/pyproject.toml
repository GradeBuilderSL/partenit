[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "partenit-decision-log"
version = "0.1.0"
description = "DecisionPacket logger, storage, and verification for Partenit"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
dependencies = [
    "pydantic>=2.0",
    "jsonlines>=3.0",
    "partenit-core>=0.1.0",
]

[project.scripts]
partenit-log    = "partenit.decision_log.cli:main"
partenit-replay = "partenit.decision_log.cli:replay_main"
partenit-record = "partenit.decision_log.cli:record_main"
partenit-why    = "partenit.decision_log.cli:why_main"
partenit-watch  = "partenit.decision_log.cli:watch_main"
partenit-stats  = "partenit.decision_log.cli:stats_main"
partenit-export = "partenit.decision_log.cli:export_main"

[tool.hatch.build.targets.wheel]
packages = ["src/partenit"]

[tool.hatch.envs.default]
dependencies = ["pytest>=7", "pytest-cov"]

[tool.hatch.envs.default.scripts]
test = "pytest tests/ -v"
